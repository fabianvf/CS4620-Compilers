bool d;

void main(){
    int c;
    int i;
    string s;
    bool b;

    i = 1; 
    s = "hello";
    c = 1 + 2 * 3 / -1;
    b = c < 10;
    b = b && (c > 10);
    d = b && !b;
    d = "hello" == s;
    Print(s);
    Print(d);
}
